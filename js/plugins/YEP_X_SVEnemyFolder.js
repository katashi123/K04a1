//=============================================================================
// Agu Plugins - Animated Sideview Enemies Extension - Enemy Folder
// YEP_X_SVEnemyFolder.js
//=============================================================================

var Imported = Imported || {};
Imported.YEP_X_SVEnemyFolder = true;

//=============================================================================
 /*:
 * @plugindesc v1.01 (Requires YEP_X_AnimatedSVEnemies.js) This plugin lets
 * you use have folders for Sideview Enemies!
 * @author Agustin
 *
 * @param ---Folder 1---
 * @default
 *
 * @param Enemy Folder 1
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 1
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M1]  (there is a space by the way :))
 * @default [M1] 
 *
 * @param ---Folder 2---
 * @default
 *
 * @param Enemy Folder 2
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 2
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M2]  (there is a space by the way :))
 * @default [M2] 
 *
 * @param ---Folder 3---
 * @default
 *
 * @param Enemy Folder 3
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 3
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M3]  (there is a space by the way :))
 * @default [M3] 
 *
 * @param ---Folder 4---
 * @default
 *
 * @param Enemy Folder 4
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 4
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M4]  (there is a space by the way :))
 * @default [M4] 
 *
 * @param ---Folder 5---
 * @default
 *
 * @param Enemy Folder 5
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 5
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M5]  (there is a space by the way :))
 * @default [M5] 
 *
 * @param ---Folder 6---
 * @default
 *
 * @param Enemy Folder 6
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 6
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M6]  (there is a space by the way :))
 * @default [M6] 
 *
 * @param ---Folder 7---
 * @default
 *
 * @param Enemy Folder 7
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 7
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M7]  (there is a space by the way :))
 * @default [M7] 
 *
 * @param ---Folder 8---
 * @default
 *
 * @param Enemy Folder 8
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 8
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M8]  (there is a space by the way :))
 * @default [M8] 
 *
 * @param ---Folder 9---
 * @default
 *
 * @param Enemy Folder 9
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 9
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M9]  (there is a space by the way :))
 * @default [M9] 
 *
 * @param ---Folder 10---
 * @default
 *
 * @param Enemy Folder 10
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 10
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M10]  (there is a space by the way :))
 * @default [M10] 
 *
 * @param ---Folder 11---
 * @default
 *
 * @param Enemy Folder 11
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 11
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M11]  (there is a space by the way :))
 * @default [M11] 
 *
 * @param ---Folder 12---
 * @default
 *
 * @param Enemy Folder 12
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 12
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M12]  (there is a space by the way :))
 * @default [M12] 
 *
 * @param ---Folder 13---
 * @default
 *
 * @param Enemy Folder 13
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 13
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M13]  (there is a space by the way :))
 * @default [M13] 
 *
 * @param ---Folder 14---
 * @default
 *
 * @param Enemy Folder 14
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 14
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M14]  (there is a space by the way :))
 * @default [M14] 
 *
 * @param ---Folder 15---
 * @default
 *
 * @param Enemy Folder 15
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 15
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M15]  (there is a space by the way :))
 * @default [M15] 
 *
 * @param ---Folder 16---
 * @default
 *
 * @param Enemy Folder 16
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 16
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M16]  (there is a space by the way :))
 * @default [M16] 
 *
 * @param ---Folder 17---
 * @default
 *
 * @param Enemy Folder 17
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 17
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M17]  (there is a space by the way :))
 * @default [M17] 
 *
 * @param ---Folder 18---
 * @default
 *
 * @param Enemy Folder 18
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 18
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M18]  (there is a space by the way :))
 * @default [M18] 
 *
 * @param ---Folder 19---
 * @default
 *
 * @param Enemy Folder 19
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 19
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M19]  (there is a space by the way :))
 * @default [M19] 
 *
 * @param ---Folder 20---
 * @default
 *
 * @param Enemy Folder 20
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 20
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M20]  (there is a space by the way :))
 * @default [M20] 
 *
 *
 * @param ---Folder 21---
 * @default
 *
 * @param Enemy Folder 21
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 21
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M21]  (there is a space by the way :))
 * @default [M21] 
 *
 * @param ---Folder 22---
 * @default
 *
 * @param Enemy Folder 22
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 22
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M22]  (there is a space by the way :))
 * @default [M22] 
 *
 * @param ---Folder 23---
 * @default
 *
 * @param Enemy Folder 23
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 23
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M23]  (there is a space by the way :))
 * @default [M23] 
 *
 * @param ---Folder 24---
 * @default
 *
 * @param Enemy Folder 24
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 24
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M24]  (there is a space by the way :))
 * @default [M24] 
 *
 * @param ---Folder 25---
 * @default
 *
 * @param Enemy Folder 25
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 25
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M25]  (there is a space by the way :))
 * @default [M25] 
 *
 * @param ---Folder 26---
 * @default
 *
 * @param Enemy Folder 26
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 26
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M26]  (there is a space by the way :))
 * @default [M26] 
 *
 * @param ---Folder 27---
 * @default
 *
 * @param Enemy Folder 27
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 27
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M27]  (there is a space by the way :))
 * @default [M27] 
 *
 * @param ---Folder 28---
 * @default
 *
 * @param Enemy Folder 28
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 28
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M28]  (there is a space by the way :))
 * @default [M28] 
 *
 * @param ---Folder 29---
 * @default
 *
 * @param Enemy Folder 29
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 29
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M29]  (there is a space by the way :))
 * @default [M29] 
 *
 * @param ---Folder 30---
 * @default
 *
 * @param Enemy Folder 30
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 30
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M30]  (there is a space by the way :))
 * @default [M30] 
 *
 * @param ---Folder 31---
 * @default
 *
 * @param Enemy Folder 31
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 31
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M31]  (there is a space by the way :))
 * @default [M31] 
 *
 * @param ---Folder 32---
 * @default
 *
 * @param Enemy Folder 32
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 32
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M32]  (there is a space by the way :))
 * @default [M32] 
 *
 * @param ---Folder 33---
 * @default
 *
 * @param Enemy Folder 33
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 33
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M33]  (there is a space by the way :))
 * @default [M33] 
 *
 * @param ---Folder 34---
 * @default
 *
 * @param Enemy Folder 34
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 34
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M34]  (there is a space by the way :))
 * @default [M34] 
 *
 * @param ---Folder 35---
 * @default
 *
 * @param Enemy Folder 35
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 35
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M35]  (there is a space by the way :))
 * @default [M35] 
 *
 * @param ---Folder 36---
 * @default
 *
 * @param Enemy Folder 36
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 36
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M36]  (there is a space by the way :))
 * @default [M36] 
 *
 * @param ---Folder 37---
 * @default
 *
 * @param Enemy Folder 37
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 37
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M37]  (there is a space by the way :))
 * @default [M37] 
 *
 * @param ---Folder 38---
 * @default
 *
 * @param Enemy Folder 38
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 38
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M38]  (there is a space by the way :))
 * @default [M38] 
 *
 * @param ---Folder 39---
 * @default
 *
 * @param Enemy Folder 39
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 39
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M39]  (there is a space by the way :))
 * @default [M39] 
 *
 * @param ---Folder 40---
 * @default
 *
 * @param Enemy Folder 40
 * @desc The location of the animated enemies. 
 * Default: img\sv_actors\
 * @default img\sv_actors\
 *
 * @param Image Prefix 40
 * @desc The prefix for animated enemy images. Note: there needs to be a prefix.
 * Default: [M40]  (there is a space by the way :))
 * @default [M40] 
 * @help
 * ============================================================================
 * Introduction
 * ============================================================================
 * In order to have your enemies be loaded in a different folder, put the prefix
 * listed in the prefix parameter before the name of the image. For example, if my
 * prefix is "[M] " and my normal image name is "Slime", my new image name would be
 * "[M] Slime". Notice how the space isn't ignored. :) Then you would put it in the 
 * enemy folder listed in the parameters. Have fun game making and I hope this 
 * plugin helps. Each prefix has a corresponding image folder.
 *
 * -Agustin
 *
 * ============================================================================
 * Changelog
 * ============================================================================
 * Version 1.01:
 * - Added support for multiple folders.
 *
 * Version 1.00a:
 * - Fixed a plugin-breaking bug.
 *
 * Version 1.00:
 * - Finished plugin.
*/
//=============================================================================

if(Imported.YEP_X_AnimatedSVEnemies) {

//=============================================================================

Yanfly.Parameters = PluginManager.parameters('YEP_X_SVEnemyFolder');
Yanfly.Param = Yanfly.Param || {};

Yanfly.Param.SVEFolder = []; //String(Yanfly.Parameters['Enemy Folder 1']);
Yanfly.Param.SVEPrefix = []; //String(Yanfly.Parameters['Image Prefix 1']);

var folders = 40;
for(var i = 0; i < folders; i++) {
	Yanfly.Param.SVEFolder[i] = String(Yanfly.Parameters['Enemy Folder '+(i+1)]);
	Yanfly.Param.SVEPrefix[i] = String(Yanfly.Parameters['Image Prefix '+(i+1)]);
}

//=============================================================================

ImageManager.loadSvActor = function(filename, hue) {
	var prefixes = Yanfly.Param.SVEPrefix;
	console.log(Yanfly.Param.SVEFolder);
	console.log(Yanfly.Param.SVEPrefix);
	for(var i = 0; i < folders; i++) {
		if(filename.substring(0,prefixes[i].length)==prefixes[i]) {
			return this.loadBitmap(Yanfly.Param.SVEFolder[i], filename, hue, true);
		}	
	}
	return this.loadBitmap('img/sv_actors/', filename, hue, true);
	
};

}

//=============================================================================
// End of File
//=============================================================================